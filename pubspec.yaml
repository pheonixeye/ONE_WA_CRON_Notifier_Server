name: one_sms_notification_server
description: ProKliniK-One SMS Notifications Server
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  dart_frog: ^1.1.0
  equatable: ^2.0.8
  http: ^1.6.0
  pocketbase: ^0.23.2
  shelf_cors_headers: ^0.1.5
  uuid: ^4.5.2

dev_dependencies:
  dart_frog_lint: ^0.1.0
  mocktail: ^1.0.3
  test: ^1.25.5
